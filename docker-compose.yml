version: '3.8'

services:
  web:
    build: ./Events
    command: python manage.py runserver 0.0.0.0:8080
    volumes:
      - ./Events/:/usr/src/app/
    ports:
      - 8080:8080
    env_file:
      - ./.env.dev